{
  "Parameters": {
    "commands": [
      "#!/bin/bash",
      "sudo cp -f /var/MagicOnion.Benchmark/client/Benchmark.Client.service /etc/systemd/system/.",
      "sudo systemctl enable Benchmark.Client",
      "sudo systemctl status Benchmark.Client",
      "sudo systemctl list-unit-files --type=service"
    ]
  }
}
