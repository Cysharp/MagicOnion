{
  "Parameters": {
    "commands": [
      "#!/bin/bash",
      "mkdir -p ~/client",
      "aws s3 sync s3://bench-magiconion-s3-bucket-5c7e45b/assembly/linux/client/ ~/client",
      "sudo chmod +x ~/server/Benchmark.Client",
      "ls -lR ~/"
    ]
  }
}
