{
  "Parameters": {
    "commands": [
      "#!/bin/bash",
      "export DOTNET_CLI_HOME=/tmp",
      "dotnet --version",
      "BENCHCLIENT_RUNASWEB=false ~/client/Benchmark.Client benchmarkrunner runallclient -processcount 5 -executecount 1000 -benchcommand benchunary -hostAddress http://localhost:80"
    ]
  }
}
