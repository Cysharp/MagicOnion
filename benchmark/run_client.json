{
  "Parameters": {
    "commands": [
      "#!/bin/bash",
      "export DOTNET_CLI_HOME=/tmp",
      "sudo killall Benchmark.Client || true",
      "dotnet --version",
      "~/client/Benchmark.Client http://localhost:80 10000"
    ]
  }
}
