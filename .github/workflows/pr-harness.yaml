name: PR Harness

on:
  pull_request:
    branches: [main]
  push:
    branches: [harness]

jobs:
  harness:
    permissions:
      contents: read
      pull-requests: read
    uses: Cysharp/Actions/.github/workflows/pr-harness.yaml@main
    with:
      license-check: true
